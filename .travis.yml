language: node_js

cache:
  directories:
    - ~/.npm

notifications:
  email: false

node_js:
  - '8'

before_install:
  - npm install -g npm@5

before_script:
  - npm prune

script:
  - npx sync-glitch-cli

branches:
  only:
    - master
